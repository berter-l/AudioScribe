[project]
name = "main-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "berter-l", email = "base.github.w@gmail.com" }
]
requires-python = "==3.11.*"
dependencies = [
    "black>=26.5.1",
    "fastapi>=0.141.1",
    "uvicorn>=0.53.0",
]

[project.scripts]
main-app = "main_app:main"

