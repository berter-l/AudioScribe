# AudioScribe

**AudioScribe** — это модульная система для автоматического захвата, транскрибации аудиозаписей и генерации
структурированных конспектов лекций, встреч или семинаров с помощью искусственного интеллекта. 

Проект построен на микросервисной архитектуре и включает в себя веб-интерфейс, Telegram-бот, браузерное расширение
для записи звука прямо из вкладок, а также распределенный воркер для высокопроизводительного распознавания речи.

## Архитектура проекта

Проект разделен на три ключевых сервиса и клиентское расширение:

— **orchestrator (FastAPI)**: Центральный узел системы, который принимает аудиофайлы, сохраняет их метаданные
в базу данных PostgreSQL, отправляет задачи в очередь RabbitMQ и обрабатывает вебхуки с результатами транскрибации.
Он же взаимодействует с LLM для форматирования текста в итоговый конспект.

— **ai_app (Celery Worker)**: Фоновый воркер, который забирает задачи из RabbitMQ, скачивает аудиофайлы из S3,
выполняет локальное распознавание речи с использованием библиотеки faster-whisper и отправляет сырой текст
обратно в оркестратор.

— **main_app (FastAPI)**: Легковесный веб-интерфейс, предоставляющий пользователям удобную HTML-страницу
для ручной загрузки аудиозаписей и просмотра списка обработанных файлов.

— **tg_app (Aiogram3)**: Telegram-бот, позволяющий отправлять аудиофайлы напрямую через мессенджер для последующей
обработки в оркестраторе.

— **browser_extension**: Браузерное расширение (Manifest V3), способное захватывать системный аудиопоток из любой
активной вкладки браузера, локально сохранять копию и автоматически пересылать её на сервер.

## Лицензия и ограничения

**ВАЖНО**: Этот проект предназначен исключительно для некоммерческого использования (Non-Commercial Use Only).
Использование данного программного обеспечения, его компонентов или производных продуктов в коммерческих целях,
в бизнесе или для извлечения прибыли строго запрещено.

## Требования для запуска

Перед запуском системы необходимо подготовить следующие внешние интеграции:

1. **API ключ LLM модели**: Для работы оркестратора и генерации конспектов требуется настроить подключение
к OpenAI-совместимому API (в проекте по умолчанию используется провайдер NeuralDeep). Вам необходимо указать рабочий
api_key в коде или переменных окружения оркестратора.

2. **S3 Хранилище**: Системе необходимо объектное хранилище (совместимое с AWS S3, например Cloud.ru, MinIO
или Яндекс Облако) для обмена аудиофайлами между оркестратором и ИИ-воркером, а также для хранения готовых текстовых
файлов конспектов.

3. **Прокси (Желательно, но не обязательно)**: Для стабильного соединения с серверами Telegram в tg_app рекомендуется
настроить прокси-сервер, однако бот может функционировать и без него, если прямая связь с API Telegram доступна.


## Конфигурация и переменные окружения

Для управления настройками используются конфигурационные файлы Pydantic Settings внутри каждого сервиса.

### Настройки Базы Данных (orchestrator/app/config/db_conf.py)
— `port`: Порт PostgreSQL (по умолчанию 5432)
— `username`: Имя пользователя БД
— `password`: Пароль пользователя БД
— `host`: Хост базы данных
— `database_name`: Имя базы данных

### Настройки RabbitMQ (orchestrator/app/config/rabbitmq_conf.py, ai_app/config/rabbitmq_conf.py)
— `port`: Порт RabbitMQ (по умолчанию 5672)
— `host`: Хост брокера сообщений
— `username`: Имя пользователя брокера
— `password`: Пароль пользователя брокера

### Настройки S3 Хранилища (orchestrator/app/config/s3_conf.py, ai_app/config/s3_conf.py)
— `region_name`: Регион S3 (например, ru-central-1)
— `aws_access_key_id`: Идентификатор ключа доступа
— `aws_secret_access_key`: Секретный ключ доступа
— `tenant_id`: Идентификатор тенанта (при необходимости)
— `bucket_name`: Имя бакета для аудиофайлов
— `domain_bucket_name`: Доменное имя бакета для генерации публичных ссылок

### Настройки Whisper AI (ai_app/config/whisper_ai_conf.py)
— `model_name`: Размер используемой модели (по умолчанию small)
— `number_of_threads`: Количество выделенных CPU потоков (по умолчанию 10)
— `device_type`: Тип устройства для вычислений (cpu или cuda)
— `compute_type`: Тип точности данных (по умолчанию int8)

## Быстрый старт через Docker Compose

Проект полностью контейнеризирован. Вы можете развернуть всю инфраструктуру (включая PostgreSQL и RabbitMQ) одной командой.

### Установка зависимостей и сборка

Убедитесь, что у вас установлены Docker и Docker Compose. В корневой директории проекта выполните команду:

```bash
docker-compose up --build
```

Docker Compose автоматически соберет и запустит следующие контейнеры:
— `db`: СУБД PostgreSQL 18.6
— `rabbitmq`: Брокер сообщений RabbitMQ 3.11
— `orchestrator`: Основной бэкенд на порту 8000
— `main_app`: Фронтенд-интерфейс на порту 9000
— `whisper_ai_app`: Celery-воркер для транскрибации
— `tg_bot_app`: Бот для взаимодействия через Telegram

## Установка браузерного расширения

1. Откройте браузер (Chrome, Edge или любой другой на движке Chromium).
2. Перейдите на страницу расширений (chrome://extensions/).
3. Включите **Режим разработчика** (Developer mode) в правом верхнем углу.
4. Нажмите кнопку **Загрузить распакованное расширение** (Load unpacked) в левом верхнем углу.
5. Выберите папку browser_extension из данного репозитория.

После установки вы сможете кликнуть на иконку расширения во время воспроизведения аудио на любой вкладке,
ввести желаемое имя, и расширение запишет звук и автоматически отправит его на ваш локальный сервер для создания конспекта.